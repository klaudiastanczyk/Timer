

const Timer = props => {

  const start = () => {

  }

  return (
    <div>
    <div>tu bedzie czas</div>
      <button onClick={startTime}>Start</button>
      <button onClick={stopTime}>STOP</button>
      <button onClick={resetTime}>RESET</button>
    </div>
  )
}

export default Timer;